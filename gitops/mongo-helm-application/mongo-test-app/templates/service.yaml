apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongo-test-app.fullname" . }}
  labels:
    {{- include "mongo-test-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongo-test-app.selectorLabels" . | nindent 4 }}
